%h2 Top commenters of the last 7 days
%table.table
  %thead
    %tr
      %th{:scope => "col"} #
      %th{:scope => "col"} User ID
      %th{:scope => "col"} User name
      %th{:scope => "col"} Comments (last 7 days)
  %tbody
    - @top_commenters.each_with_index do |user, i|
      %tr
        %th{:scope => "row"}
          = i + 1
        %td
          = user.id
        %td
          = user.name
        %td
          = user.comments.recents.count